{"queries":[{"id":"available_models","compiledQueryString":"select distinct model_name\nfrom motherduck.food_table_allergens_flat_prediction_average\nwhere user_hash_id = '${params.user_hash_id}'\norder by model_name","inputQueryString":"select distinct model_name\nfrom motherduck.food_table_allergens_flat_prediction_average\nwhere user_hash_id = '${params.user_hash_id}'\norder by model_name","compiled":false,"inline":true},{"id":"food_table_allergens_prediction_average_points","compiledQueryString":"select\n  *,\n  '/users/${params.user_hash_id}/allergen/' || allergen as allergen_link\nfrom motherduck.food_table_allergens_flat_prediction_average\nwhere user_hash_id = '${params.user_hash_id}'\n  and ('${inputs.model_filter}' IN ('', 'undefined', 'null') OR model_name = '${inputs.model_filter.value}')\n  and allergen is not null and allergen != ''","inputQueryString":"select\n  *,\n  '/users/${params.user_hash_id}/allergen/' || allergen as allergen_link\nfrom motherduck.food_table_allergens_flat_prediction_average\nwhere user_hash_id = '${params.user_hash_id}'\n  and ('${inputs.model_filter}' IN ('', 'undefined', 'null') OR model_name = '${inputs.model_filter.value}')\n  and allergen is not null and allergen != ''","compiled":false,"inline":true},{"id":"allergens_prediction_unpivoted","compiledQueryString":"UNPIVOT (select\n  *,\n  '/users/${params.user_hash_id}/allergen/' || allergen as allergen_link\nfrom motherduck.food_table_allergens_flat_prediction_average\nwhere user_hash_id = '${params.user_hash_id}'\n  and ('${inputs.model_filter}' IN ('', 'undefined', 'null') OR model_name = '${inputs.model_filter.value}')\n  and allergen is not null and allergen != '')\nON average_state_point, average_sleep_point, average_bloating_point, average_reflux_point, average_finger_point\nINTO\n    NAME state_name\n    VALUE average_value;","inputQueryString":"UNPIVOT ${food_table_allergens_prediction_average_points}\nON average_state_point, average_sleep_point, average_bloating_point, average_reflux_point, average_finger_point\nINTO\n    NAME state_name\n    VALUE average_value;","compiled":true,"inline":true},{"id":"allergen_sleep_prompt","compiledQueryString":"select 'What can be in common in these allergens that make me sleepy? ' ||\n       string_agg(allergen || '-' || round(average_sleep_point, 2)::text, ', ' order by average_sleep_point desc) as prompt\nfrom (select\n  *,\n  '/users/${params.user_hash_id}/allergen/' || allergen as allergen_link\nfrom motherduck.food_table_allergens_flat_prediction_average\nwhere user_hash_id = '${params.user_hash_id}'\n  and ('${inputs.model_filter}' IN ('', 'undefined', 'null') OR model_name = '${inputs.model_filter.value}')\n  and allergen is not null and allergen != '')","inputQueryString":"select 'What can be in common in these allergens that make me sleepy? ' ||\n       string_agg(allergen || '-' || round(average_sleep_point, 2)::text, ', ' order by average_sleep_point desc) as prompt\nfrom ${food_table_allergens_prediction_average_points}","compiled":true,"inline":true},{"id":"allergen_state_prompt","compiledQueryString":"select 'What can be in common in these allergens that affect my mental state? ' ||\n       string_agg(allergen || '-' || round(average_state_point, 2)::text, ', ' order by average_state_point desc) as prompt\nfrom (select\n  *,\n  '/users/${params.user_hash_id}/allergen/' || allergen as allergen_link\nfrom motherduck.food_table_allergens_flat_prediction_average\nwhere user_hash_id = '${params.user_hash_id}'\n  and ('${inputs.model_filter}' IN ('', 'undefined', 'null') OR model_name = '${inputs.model_filter.value}')\n  and allergen is not null and allergen != '')","inputQueryString":"select 'What can be in common in these allergens that affect my mental state? ' ||\n       string_agg(allergen || '-' || round(average_state_point, 2)::text, ', ' order by average_state_point desc) as prompt\nfrom ${food_table_allergens_prediction_average_points}","compiled":true,"inline":true},{"id":"allergen_bloating_prompt","compiledQueryString":"select 'What can be in common in these allergens that cause me bloating? ' ||\n       string_agg(allergen || '-' || round(average_bloating_point, 2)::text, ', ' order by average_bloating_point desc) as prompt\nfrom (select\n  *,\n  '/users/${params.user_hash_id}/allergen/' || allergen as allergen_link\nfrom motherduck.food_table_allergens_flat_prediction_average\nwhere user_hash_id = '${params.user_hash_id}'\n  and ('${inputs.model_filter}' IN ('', 'undefined', 'null') OR model_name = '${inputs.model_filter.value}')\n  and allergen is not null and allergen != '')","inputQueryString":"select 'What can be in common in these allergens that cause me bloating? ' ||\n       string_agg(allergen || '-' || round(average_bloating_point, 2)::text, ', ' order by average_bloating_point desc) as prompt\nfrom ${food_table_allergens_prediction_average_points}","compiled":true,"inline":true},{"id":"allergen_reflux_prompt","compiledQueryString":"select 'What can be in common in these allergens that cause me reflux? ' ||\n       string_agg(allergen || '-' || round(average_reflux_point, 2)::text, ', ' order by average_reflux_point desc) as prompt\nfrom (select\n  *,\n  '/users/${params.user_hash_id}/allergen/' || allergen as allergen_link\nfrom motherduck.food_table_allergens_flat_prediction_average\nwhere user_hash_id = '${params.user_hash_id}'\n  and ('${inputs.model_filter}' IN ('', 'undefined', 'null') OR model_name = '${inputs.model_filter.value}')\n  and allergen is not null and allergen != '')","inputQueryString":"select 'What can be in common in these allergens that cause me reflux? ' ||\n       string_agg(allergen || '-' || round(average_reflux_point, 2)::text, ', ' order by average_reflux_point desc) as prompt\nfrom ${food_table_allergens_prediction_average_points}","compiled":true,"inline":true},{"id":"allergen_finger_prompt","compiledQueryString":"select 'What can be in common in these allergens that cause my fingers to swell? ' ||\n       string_agg(allergen || '-' || round(average_finger_point, 2)::text, ', ' order by average_finger_point desc) as prompt\nfrom (select\n  *,\n  '/users/${params.user_hash_id}/allergen/' || allergen as allergen_link\nfrom motherduck.food_table_allergens_flat_prediction_average\nwhere user_hash_id = '${params.user_hash_id}'\n  and ('${inputs.model_filter}' IN ('', 'undefined', 'null') OR model_name = '${inputs.model_filter.value}')\n  and allergen is not null and allergen != '')","inputQueryString":"select 'What can be in common in these allergens that cause my fingers to swell? ' ||\n       string_agg(allergen || '-' || round(average_finger_point, 2)::text, ', ' order by average_finger_point desc) as prompt\nfrom ${food_table_allergens_prediction_average_points}","compiled":true,"inline":true}]}